server:
  port: 6001

eureka:
  client:
    register-with-eureka: false
    #是否检索服务
    fetch-registry: false
    healthcheck:
      enabled: true
  serviceUrl:
    defaultZone: http://localhost:6001/eureka
